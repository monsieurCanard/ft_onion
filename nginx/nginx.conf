events {
		worker_connections 1024;
}


http {
	server {

		listen 80;
		server_name localhost;
		index index.html;
		root /usr/share/nginx/html;
		
		location / {
			index index.html;
		root /usr/share/nginx/html;
		}

	}
}